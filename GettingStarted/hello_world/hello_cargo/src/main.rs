fn main() {
    println!("Hello, world!");
}

// top-level is for README, license, configuration and anything else not related to the code.
// cargo init will create cargo project.

// Building and Running a Cargo Project
// Cargo.lock keeps track of the exact versions of dependencies in your project.
// cargo run = cargo build + ./main.rs
// cargo check : check code to make sure it compiles but doesn't produce an executable.
// cargo commands stay the same of Linux/macOS/Windows.

// Building for release
//
